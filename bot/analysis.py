import os
import re
import time
from datetime import datetime
from dotenv import load_dotenv
from openai import AsyncOpenAI
from bot.technical_analysis import TechnicalAnalyzer
from bot.indicators import get_technical_indicators
from aiolimiter import AsyncLimiter

load_dotenv()

# Настройка клиента (DeepSeek через OpenRouter)
client = AsyncOpenAI(
    api_key=os.getenv("OPENROUTER_API_KEY"),
    base_url="https://openrouter.ai/api/v1"
)

MODEL_NAME = "deepseek/deepseek-chat"

# --- RATE LIMITER ---
# OpenRouter Free tier: 10 req/min, используем 80% лимита для безопасности
openrouter_limiter = AsyncLimiter(8, 60)

# --- КЭШИРОВАНИЕ ---
ANALYSIS_CACHE = {}
CACHE_TTL = 300
DAILY_CACHE_TTL = 1800

# --- СЕКТОРЫ ДЛЯ МОНЕТ (корректный словарь) ---
SECTOR_MAP = {
    # AI
    "FET": "AI",
    "AGIX": "AI",
    "RNDR": "AI",
    "AKT": "AI",
    "TAO": "AI",
    "GRT": "AI",
    "Bittensor": "AI",
    # Layer-2
    "ARB": "Layer-2",
    "OP": "Layer-2",
    "STRK": "Layer-2",
    "MANTA": "Layer-2",
    "ZK": "Layer-2",
    "IMX": "Layer-2",
    "METIS": "Layer-2",
    # RWA
    "ONDO": "RWA",
    "CFG": "RWA",
    "POLYX": "RWA",
    "PROPC": "RWA",
    # DePIN
    "HNT": "DePIN",
    "WLD": "DePIN",
    "LPT": "DePIN",
    "DIMO": "DePIN",
    "TRAC": "DePIN",
    # GameFi
    "SAND": "GameFi",
    "MANA": "GameFi",
    "AXS": "GameFi",
    "GALA": "GameFi",
    "ENJ": "GameFi",
    # Memes
    "PEPE": "Meme",
    "SHIB": "Meme",
    "WIF": "Meme",
    "BONK": "Meme",
    "FLOKI": "Meme",
    # Infrastructure
    "LINK": "Infrastructure",
    "DOT": "Infrastructure",
    "ADA": "Infrastructure",
    "SOL": "Infrastructure",
    "AVAX": "Infrastructure",
    "MATIC": "Infrastructure",
}

def get_sector(ticker):
    """Определяет сектор по тикеру."""
    return SECTOR_MAP.get(ticker, "Other")

def clean_html(text):
    if not text: return ""
    
    # 1. Сначала экранируем спецсимволы, чтобы не сломать HTML-парсинг Telegram
    text = text.replace("&", "&amp;").replace("<", "&lt;").replace(">", "&gt;")
    
    # 2. Удаляем блоки кода (если есть), так как они не нужны в отчете
    text = re.sub(r"```.*?```", "", text, flags=re.DOTALL)
    text = text.replace("```", "").replace("markdown", "").replace("html", "")
    
    # 3. Преобразуем Markdown в HTML, поддерживаемый Telegram
    # Жирный текст: **text** -> <b>text</b>
    text = re.sub(r"\*\*(.*?)\*\*", r"<b>\1</b>", text)
    
    # Заголовки: # Text -> <b>Text</b> (обрабатываем от H3 до H1)
    text = re.sub(r"###\s*(.*)", r"<b>\1</b>", text)
    text = re.sub(r"##\s*(.*)", r"<b>\1</b>", text)
    text = re.sub(r"^#\s+(.*)", r"<b>\1</b>", text, flags=re.MULTILINE)
    
    # Списки: * Item или - Item -> • Item
    text = text.replace("* ", "• ").replace("- ", "• ")
    
    return text.strip()

# --- 1. ФУНДАМЕНТАЛЬНЫЙ АУДИТ ---
async def get_crypto_analysis(ticker, full_name, lang="ru"):
    """
    Глубокий фундаментальный анализ через AI (DeepSeek/OpenAI).
    """
    client = AsyncOpenAI(
        api_key=os.getenv("OPENROUTER_API_KEY"),
        base_url="https://openrouter.ai/api/v1",
    )
    
    model = os.getenv("MODEL_NAME", "deepseek/deepseek-chat")

    # ЖЕСТКИЙ ПРОМТ ДЛЯ ГЛУБОКОГО АНАЛИЗА
    prompt = f"""
    Ты — старший аналитик криптовалютного хедж-фонда. Твоя специализация — фундаментальный анализ (L1/L2, DeFi, Infra).
    
    ТВОЯ ЗАДАЧА: Провести глубокий аудит монеты: {ticker.upper()}.
    
    ТРЕБОВАНИЯ:
    1. ИЗБЕГАЙ общих фраз ("проект перспективный", "рынок волатилен"). Пиши факты.
    2. Используй конкретные названия технологий, имен фаундеров, цифры (TVL, TPS, Инфляция).
    3. Если токен новый (как EIGEN) и данных мало — скажи об этом прямо, проанализируй то, что есть (технологию, инвесторов).
    
    СТРУКТУРА ОТВЕТА (СТРОГО СОБЛЮДАЙ MARKDOWN):

    # 🛡 {ticker.upper()} — Фундаментальный разбор

    ## 1️⃣ Фундаментальная ценность
    * **Суть и Технологии:** Какую КОНКРЕТНУЮ проблему решает? (Назови консенсус, архитектуру, уникальные фичи).
    * **Конкурентная среда:** Кто прямые конкуренты? В чем {ticker} лучше или хуже?
    * **Команда и Инвесторы:** Кто стоит за проектом? (Имена, фонды). Есть ли доверие?

    ## 2️⃣ Токеномика и Метрики
    * **Токеномика:** Как распределены токены? Есть ли сжигание? Какая инфляция?
    * **Разлоки (Vesting):** Есть ли давление продавцов (VC, фонды)? Когда ближайшие разлоки?
    * **Активность:** Что с TVL? Активные адреса? (Если данных нет — оцени Github/Socials).

    ## 3️⃣ Макро и Риски
    * **Связь с рынком:** Корреляция с BTC. Это "High Beta" актив?
    * **Риски:** Регуляция? Технические сбои? Централизация?

    ## 🎯 Вердикт и Сценарии
    * **Базовый сценарий:** Что будет при спокойном рынке?
    * **Оптимистичный сценарий:** Условия для "Туземуна" (партнерства, тех. обновления).
    * **Пессимистичный сценарий:** Что может убить проект?
    
    Выведи итог одним предложением.
    """

    try:
        completion = await client.chat.completions.create(
            model=model,
            messages=[
                {"role": "system", "content": "Ты профессиональный крипто-аналитик. Ты пишешь структурированные, сухие, насыщенные фактами отчеты."},
                {"role": "user", "content": prompt}
            ],
            temperature=0.0  # Максимальная детерминированность, нет галлюцинаций
        )
        return clean_html(completion.choices[0].message.content)
    except Exception as e:
        return f"⚠️ Ошибка AI анализа: {str(e)}"

from bot.prices import get_market_summary, get_crypto_price

# --- 2. СНАЙПЕР (SMART MONEY / SMC) — УЛУЧШЕННАЯ ВЕРСИЯ ---
async def get_sniper_analysis(ticker, language="ru"):
    # 1. Получаем данные (Futures Only)
    price_data, error = await get_crypto_price(ticker)
    
    if not price_data:
        return f"⚠️ Не удалось получить данные по {ticker}. Возможно, тикер не торгуется на фьючерсах."

    # 2. Получаем МАТЕМАТИЧЕСКИЕ индикаторы (без галлюцинаций)
    tech_data = await get_technical_indicators(ticker)
    
    if not tech_data:
        rsi_val = "N/A"
        trend_val = "UNKNOWN"
        s1_val = "N/A"
        r1_val = "N/A"
    else:
        rsi_val = tech_data['rsi']
        trend_val = tech_data['trend']
        s1_val = tech_data['support']
        r1_val = tech_data['resistance']

    # 3. Безопасное извлечение данных
    curr_price = price_data.get('price', 'N/A')
    change = price_data.get('change_24h', 'N/A')
    vol = price_data.get('volume_24h', 'N/A')
    source = price_data.get('source', 'Unknown Futures')
    
    # 4. СУПЕР-ПРОМТ (Real Data)
    prompt = f"""
    Ты — элитный трейдер (Smart Money / ICT Concepts). Твоя специализация — снайперские входы.
    Язык ответа: {language.upper()} (Russian).
    
    ВХОДНЫЕ ДАННЫЕ (ФАКТЫ):
    • АКТИВ: {ticker.upper()}
    • ЦЕНА: ${curr_price} ({change}%)
    • RSI (14): {rsi_val} (Если > 70 — перекуплен/шорт, если < 30 — перепродан/лонг)
    • ТРЕНД (SMA20): {trend_val}
    • ПОДДЕРЖКА (Low 50h): ${s1_val}
    • СОПРОТИВЛЕНИЕ (High 50h): ${r1_val}
    
    ТВОЯ ЗАДАЧА:
    Проведи анализ на основе ЭТИХ цифр. Не выдумывай уровни, используй S1/R1 как базу.
    Структура ответа (Clean UI, без Markdown заголовков):

    📊 <b>{ticker.upper()} | Smart Money Setup</b>
    💰 Цена: ${curr_price}
    📉 RSI: {rsi_val} | Тренд: {trend_val}

    1️⃣ <b>Структура рынка</b>
    ▪️ Фаза: [ACCUMULATION / DISTRIBUTION] (Суди по RSI и тренду)
    📡 Ключевые уровни (ФАКТ):
       ▫️ Support (S1): ${s1_val}
       ▫️ Resistance (R1): ${r1_val}

    2️⃣ <b>Ликвидность & Sentiment</b>
    ▪️ Зона интереса: [Где скопление ликвидности?]
    ▪️ Настроение: [Страх/Жадность]

    3️⃣ <b>P-Score (Вероятность): [0-100]%</b>
    ▪️ Обоснование: [Почему такой %?]

    🎯 <b>Снайперский план</b>
    🔵 Тип: [LONG / SHORT]
    🚪 Вход: [Цена] (Ищи вход от S1 или R1)
    🛡 Стоп: [Цена]
    ✅ Тейки: [TP1, TP2, TP3]
    
    ⚖️ <b>Совет ММ:</b> Риск 1% на сделку.
    """

    try:
        # 5. Защита Rate Limiter
        async with openrouter_limiter:
            completion = await client.chat.completions.create(
                model=os.getenv("MODEL_NAME", "deepseek/deepseek-chat"),
                messages=[
                    {"role": "system", "content": "Ты профессиональный крипто-аналитик. Ты анализируешь ФАКТИЧЕСКИЕ данные. Ты не придумываешь цифры."},
                    {"role": "user", "content": prompt}
                ],
                temperature=0.0  # Максимальная детерминированность для технических уровней
            )
        return completion.choices[0].message.content
    except Exception as e:
        print(f"Sniper AI Error: {e}")
        return f"⚠️ Ошибка анализа: {e}"

# --- 3. DAILY BRIEFING — ГЛУБОКИЙ АНАЛИЗ ПО СЕКТОРАМ ---
async def get_daily_briefing(market_data=None):
    try:
        date_str = datetime.now().strftime("%d.%m.%Y")
        cache_key = f"daily_briefing_{date_str}"
        
        if cache_key in ANALYSIS_CACHE:
            timestamp, cached_text = ANALYSIS_CACHE[cache_key]
            if time.time() - timestamp < DAILY_CACHE_TTL:
                return cached_text

        # Если market_data не передан, получаем его
        if not market_data:
            try:
                market_data = await get_market_summary()
            except Exception as e:
                print(f"Error fetching market summary: {e}")
                market_data = {}

        # Формируем строку с данными рынка
        market_data_str = market_data.get('top_coins', 'BTC: $96000, ETH: $2700')

        system_prompt = f""" 
    Ты — профессиональный аналитик деривативов и фьючерсного рынка.
    Твоя задача — найти и проанализировать **ТРИ (3)** наиболее перспективные монеты на текущем рынке для фьючерсной сделки.
 
    ВХОДНЫЕ ДАННЫЕ (Контекст рынка): 
    {market_data_str} 
 
    ИСПОЛЬЗУЙ СЛЕДУЮЩУЮ ЛОГИКУ И КРИТЕРИИ:
 
    1. **Общие критерии поиска:**
       * **Ликвидность:** Монеты должны быть достаточно ликвидными (Топ-200, высокий объем на Binance/Bybit).
       * **Волатильность:** Достаточная для получения прибыли (избегай "мертвых" монет).
       * **Доступность:** Наличие фьючерсных контрактов.
 
    2. **Основные триггеры (один или несколько):**
       * **Технические сигналы:** Паттерны (Двойное дно/вершина, Голова и плечи), уровни S/R, аномальные объемы, границы консолидации.
       * **Индикаторы настроения:** Дисбаланс Long/Short (сквизы), ставки финансирования (Funding Rate), скопления ликвидности (Stop Hunt).
       * **Фундаментал:** Обновления, партнерства, листинги, новости.
 
    3. **Приоритетность:**
       * Фокусируйся на монетах с четкими сигналами (LONG или SHORT).
       * Выбирай монеты с разной степенью риска, если возможно.
 
    ФОРМАТ ВЫВОДА (Markdown):
 
    # 📊 Daily Futures Top-3 Setups
 
    ## 1️⃣ [ТИКЕР] — [LONG/SHORT]
    * **Цена:** $...
    * **Ключевой триггер:** (Почему выбрана эта монета? Напр: "Сбор ликвидности + Тест уровня")
    * **Технический анализ:**
      * **Паттерн/Структура:** ...
      * **Зоны интереса:** Support: $... | Resistance: $...
    * **Настроение/Объемы:** (Funding, OI, Sentiment)
    * **Торговый план:**
      * Вход: $...
      * Цели (TP): $... / $...
      * Стоп (SL): $...
 
    ## 2️⃣ [ТИКЕР] — [LONG/SHORT]
    ... (аналогичная структура)
 
    ## 3️⃣ [ТИКЕР] — [LONG/SHORT]
    ... (аналогичная структура)
 
    ⚠️ _Дисклеймер: Соблюдайте риск-менеджмент. Не является финансовой рекомендацией._
    """

        try:
            async with openrouter_limiter:
                response = await client.chat.completions.create(
                    model=MODEL_NAME,
                    messages=[
                        {"role": "system", "content": "You are a crypto analyst. Output in Markdown."},
                        {"role": "user", "content": system_prompt}
                    ],
                    temperature=0.0,  # Максимальная точность
                    extra_headers={"HTTP-Referer": "https://telegram.org", "X-Title": "CryptoBot"}
                )
                result = clean_html(response.choices[0].message.content)
            
            if not result:
                 return "⚠️ Не удалось получить ответ от нейросети. Попробуйте еще раз через минуту."

            ANALYSIS_CACHE[cache_key] = (time.time(), result)
            return result
        except Exception as e:
            print(f"LLM Error: {e}")
            return "⚠️ Не удалось получить ответ от нейросети. Попробуйте еще раз через минуту."

    except Exception as e:
        print(f"Critical Error in daily briefing: {e}")
        return f"⚠️ Произошла внутренняя ошибка при формировании брифинга. Попробуйте позже."